import {countrydropdown, scrollFunction, topFunction} from "./js/subButtons";

describe("Testing the countrydropdown() functionality", () => {
    test("Testing the countrydropdown() function", () => {
        expect(countrydropdown).toBeDefined();
    })});


describe("Testing the scrollFunction() functionality", () => {
    test("Testing the scrollFunction() function", () => {
        expect(scrollFunction).toBeDefined();
    })});

describe("Testing the topFunction() functionality", () => {
    test("Testing the topFunction() function", () => {
        expect(topFunction).toBeDefined();
    })});    

